# Context Compiler — active dependencies
networkx>=3.1

# Phase 5 scorer (edge-level MLP)
torch>=2.1.0
numpy>=1.24.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
tqdm>=4.66.0
pandas>=2.0.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Legacy ast-graph-transformer dependencies (NOT needed by the current system)
# If you need them, install separately:
#   pip install torch_geometric tree_sitter==0.21.3 tree_sitter_languages==1.10.2
pyflakes==3.4.0
tiktoken>=0.5.0
sentence-transformers>=2.2.0
